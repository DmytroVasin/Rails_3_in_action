<legend>
	<h4><%= link_to "New User", new_admin_user_path, class: 'btn btn-primary btn_back' %><h4>
</legend>

<ul class="nav nav-list">
	<% @users.each do |user| %>
		<li><%= link_to user, [:admin, user] %></li>
	<% end %>
</ul>
